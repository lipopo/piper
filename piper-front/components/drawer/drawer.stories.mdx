import { Meta, Preview, Story } from '@storybook/addon-docs/blocks';
import Drawer from './Drawer.vue';


<Meta title="component/Drawer" />


## Drawer

抽屉相关的组件

### BaseDrawer

基础的抽屉组件，用于描述主要的抽屉信息

<Preview>
    <Story name="base-drawer">{
        ((args, { argTypes }) => ({
        components: {
            Drawer
        },
        template: `
        <div>
          <Drawer :open='open' :style='{border: "1px solid #ccc"}'>
              <div>This is A Drawer</div>
              <div slot='header'><button @click='switch_drawer'>switch</button></div>
          </Drawer>
          <Drawer position='left' :open='open' :style='{border: "1px solid #ccc"}'>
              <div>This is A Drawer</div>
              <div slot='header'><button @click='switch_drawer'>switch</button></div>
          </Drawer>
        </div>
        `,
        methods: {
            switch_drawer: function() {
                this.open = !this.open;
            }
        },
        data: function() {
            return {
                open: true
            };
        }
    })).bind({})}</Story>
</Preview>
